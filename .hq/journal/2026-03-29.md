# Session — 2026-03-29

## What happened
- Frontend rewrite: 648-line prototype → 1946-line production design with design token system, Plus Jakarta Sans, achromatic dark theme
- Vercel deployment: `main.py` → `api/index.py`, added `vercel.json`, `requirements.txt`, conditional DB path
- Added "Next" roadmap items: custom icons, intermediate scores

## Decisions
- Directory restructure (`api/`, `public/`) follows Vercel Python serverless conventions
- Ephemeral `/tmp/games.db` on Vercel — good enough for dogfooding
- CSS custom property token system over ad-hoc values
